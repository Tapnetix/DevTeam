apiVersion: v1
kind: Service
metadata:
  name: slack-bot
  namespace: devteam
  labels:
    app.kubernetes.io/name: slack-bot
    app.kubernetes.io/part-of: devteam
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  selector:
    app.kubernetes.io/name: slack-bot
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-bot
  namespace: devteam
  labels:
    app.kubernetes.io/name: slack-bot
    app.kubernetes.io/part-of: devteam
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: slack-bot
  template:
    metadata:
      labels:
        app.kubernetes.io/name: slack-bot
        app.kubernetes.io/part-of: devteam
    spec:
      containers:
        - name: slack-bot
          image: devteam/slack-bot:latest
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: devteam-config
            - secretRef:
                name: devteam-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 3
